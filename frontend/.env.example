# Only needed when the dashboard is NOT served behind the same origin as the
# API. In development the Vite dev server proxies /api to the backend, so this
# can stay empty.
VITE_API_BASE_URL=

# Where the dev-server proxy forwards /api (defaults to http://localhost:8000).
VITE_API_TARGET=http://localhost:8000
